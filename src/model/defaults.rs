use crate::model::currency::Currency;

/// Default currency to be used when not specified.
const DEFAULT_CURRENCY: Currency = Currency::EUR;
/// Default number of decimal places for accounting.
const DEFAULT_DECIMAL: u8 = 10;
